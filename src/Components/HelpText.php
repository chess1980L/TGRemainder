<?php

declare(strict_types=1);

namespace TgRemainder\Components;

/**
 * Маркированная справка по формату Excel для загрузки напоминаний.
 *
 * Замечания:
 *  - Используется классический Telegram Markdown (не MarkdownV2).
 *  - Если в тексте события есть символы Markdown (*_[]() и т.п.) — экранируйте их,
 *    иначе форматирование в сообщении может “поехать”.
 */
final class HelpText
{
    private function __construct()
    {
        // Утилитарный класс: инстанцирование не требуется.
    }

    public static function get(): string
    {
        return <<<'MD'
*🧾 Справка по загрузке Excel*

*Обязательные колонки*: `date`, `event`, `contacts`
*Опциональные*: `time`, `repeat_days`

— — —

*Колонка `date`*
• Формат: `ДД.ММ.ГГГГ` (пример: `06.08.2025`)
• Несколько дат через запятую: `06.08.2025, 07.08.2025`
• Другие форматы (`2025-08-06`, `06/08/2025`) — *не принимаются*
• Невозможные даты (например, `31.02.2025`) — *ошибка*

*Колонка `time`*
• Если пусто — берётся `DEFAULT_TIME` из `.env` (например, `12:00`)
• Допустимые варианты одного значения:
  - `H:MM` или `HH:MM` → `9:00`, `14:30`, `23:05`
  - `H.MM` или `HH.MM` → `9.00`, `14.30`, `23.05`
  - `H:M`  → `9:5` = `09:05` (с `:` одна цифра минут дополняется нулём)
  - `H.M`  → `9.5` = `09:50` (с `.` одна цифра минут — это *десятки минут*)
  - целый час → `9` = `09:00`
• Диапазоны: часы 0–23, минуты 0–59
• Несколько значений через запятую: `09:00, 14:30` или `9, 14.30`
• Примеры ошибок: `24:00`, `09:70`, `9-30`, `9:`

*Колонка `contacts`*
• Допустимы:
  - числовой `chat_id` (только цифры)
  - `username` (5–32, `a-z`, `0-9`, `_`, с `@` или без)
• Несколько контактов: перенос строки, `;` или `,`

*Колонка `repeat_days`*
• Пусто → без повтора
• Число → каждые *N* дней (пример: `3`)
• Месячный повтор → `1m`, `1м`, `1 m`, `1 м`

— — —

*Режим загрузки при отправке файла*
• **update** (по умолчанию) — добавляет события, не удаляя существующие
• **replace** — очищает все напоминания и загружает только из файла
📌 Режим указывается в подписи (caption) к файлу; без подписи используется *update*.

— — —

*Ограничения*
• Поддерживаемый формат файла: `.xlsx`
MD;
    }
}